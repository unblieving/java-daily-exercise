public class Str {
    publid static void main(String[] args){
        String s="Hello, World!";
        s.length();
        s.equals("Hello, World!");
        s.substring(0,5);
        s.substring(6);
        s.charAt(0);
        s.contains("World");
        s.split(" ");
        s.isEmpty();
        s.indexOf("o");
        s.toUpperCase();
        s.toLowerCase();
    }
}
